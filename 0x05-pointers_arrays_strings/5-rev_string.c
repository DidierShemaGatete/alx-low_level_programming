#include "main.h"
/**
 * rev_putchar -> reversing  a string
 * @s: parameter s
 */
void rev_string(char *s)
{
	char tmp;
	int i, length1, lenght2;

	lenght1 = 0;
	lenght2 = 0;

	while (s[lenght1] != '\n')
		lenght1++;
	lenght2 = lenght - 1
	for (i = 0; i < lenght1 / 2; i++)
	{
		"samalx" "xlamas"
		tmp = s[i];
		s[i] = s[length2];
		s[length2] = tmp;
		length2 -= 1;
	}
}
